# DopplerSecret for Content Service
apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: content-service-doppler-secret
  namespace: doppler-operator-system
spec:
  tokenSecret:
    name: doppler-token-secret
  project: bingeo-content
  config: dev
  managedSecret:
    name: content-service-secrets
    namespace: bingeo-app
